-- ============================================================
-- إضافة دور 'admin' لنوع user_role.
-- ده لازم يتشغل في migration لوحده (منفصل عن أي كود بيستخدم القيمة
-- دي)، لإن Postgres مش بيسمح تستخدم قيمة enum جديدة في نفس الـ
-- transaction اللي اتضافت فيها.
-- ============================================================

alter type user_role add value if not exists 'admin';
