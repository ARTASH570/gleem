-- ============================================================
-- إضافة مدة الموعد (بالدقايق) عشان نقدر نكتشف تعارض المواعيد
-- (موعدين بيتقاطعوا في نفس الوقت). المواعيد القديمة هتاخد 30
-- دقيقة افتراضيًا.
-- ============================================================

alter table appointments
  add column if not exists duration_minutes integer not null default 30
  check (duration_minutes > 0);
